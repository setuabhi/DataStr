package LeetCode.day8;

/** java calculates hashCode by below mechanism
 * int result = 1;
 * for (Object element : values)
 * {
 *   result = 31 * result + (element == null ? 0 : element.hashCode());
 * }
 */
public class HashMapInternal {
}
