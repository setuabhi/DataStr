1. Adapter, to make non-compatible class work together without modifying it. Example RazorPayAPI working with the help of RazorPayAndPaymentAdapter
2. Decorator is nothing, it will implement Main interface and add some decoration in its method, and accept object in constructor.
3. Facade, client will not know which interface being called internally
4. Facade → hides complexity of usage
   Factory → hides complexity of object creation