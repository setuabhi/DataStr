1. Static Inner Builder Class present
2. Each method of builder class will set it's field, which is same as Main Class field then return current object
3. Constructor of Main class should be called from build method of Builder class